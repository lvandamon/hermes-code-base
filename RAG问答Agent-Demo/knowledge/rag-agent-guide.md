# RAG Agent 指南

## 检索
RAG 使用向量检索、关键词检索或混合检索找到与用户问题相关的资料片段。生产系统中通常还会加入 rerank，以便把更相关的 chunk 排在前面。

## 引用
RAG 问答需要给出引用来源。引用可以包含文档路径、标题、chunk id 和章节标题。引用的作用是让用户知道答案依据来自哪里。

## 拒答
当检索结果不足、相关性太低或上下文没有覆盖用户问题时，RAG Agent 应该拒答，并提示用户补充资料，而不是编造答案。
